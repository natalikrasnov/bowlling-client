export const addScoreAction = (score) => ({
  type: "ADD_SCORE",
  score,
});

export const setScoreAction = (bonus) => ({
  type: "SET_SCORES",
  scores,
});

export const getFinalScore = () => ({
  type: "GET_FINAL_SCORE",
  roomData,
});
